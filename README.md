# Homebrew tap for cctty

Install `cctty` with:

```sh
brew install Pyiner/cctty/cctty
```

or tap once:

```sh
brew tap Pyiner/cctty
brew install cctty
```

Project repository: https://github.com/Pyiner/cctty
